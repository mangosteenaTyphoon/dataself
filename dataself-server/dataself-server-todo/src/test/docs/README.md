# OKR-Todo 系统测试文档

## 📚 文档目录

本目录包含 OKR-Todo 系统的完整测试文档，包括测试用例、测试脚本和测试数据。

### 📁 文档结构

```
test/
├── docs/                           # 测试文档
│   ├── README.md                   # 本文件
│   ├── API测试用例.md               # 完整的API测试用例
│   ├── 目标管理接口测试.md          # Goal接口详细测试
│   ├── 任务管理接口测试.md          # Task接口详细测试
│   ├── 统计分析接口测试.md          # Statistics接口详细测试
│   └── 测试数据准备.md              # 测试数据SQL
├── scripts/                        # 测试脚本
│   ├── test-all.sh                 # 完整测试脚本
│   ├── test-goal.sh                # 目标管理测试
│   ├── test-task.sh                # 任务管理测试
│   └── test-statistics.sh          # 统计分析测试
└── postman/                        # Postman集合
    └── OKR-Todo-API.postman_collection.json
```

---

## 🚀 快速开始

### 1. 准备工作

#### 1.1 启动服务
```bash
cd dataself/dataself-server/dataself-server-todo
mvn clean package -DskipTests
java -jar target/dataself-server-todo.jar
```

#### 1.2 初始化数据库
```bash
# 执行SQL脚本
mysql -u root -p your_database < src/main/resources/sql/todo_goal.sql
mysql -u root -p your_database < src/main/resources/sql/todo_task.sql
mysql -u root -p your_database < src/main/resources/sql/todo_menu.sql
```

#### 1.3 准备测试数据
```bash
# 执行测试数据SQL
mysql -u root -p your_database < src/test/docs/测试数据准备.sql
```

---

### 2. 运行测试

#### 2.1 使用Shell脚本测试
```bash
# 运行所有测试
cd src/test/scripts
chmod +x *.sh
./test-all.sh

# 运行单个模块测试
./test-goal.sh        # 测试目标管理
./test-task.sh        # 测试任务管理
./test-statistics.sh  # 测试统计分析
```

#### 2.2 使用Swagger UI测试
访问：`http://localhost:8084/swagger-ui.html`

#### 2.3 使用Postman测试
导入：`src/test/postman/OKR-Todo-API.postman_collection.json`

---

## 📊 测试覆盖

### 接口覆盖率

| 模块 | 接口数 | 测试用例数 | 覆盖率 |
|------|--------|-----------|--------|
| 目标管理 | 8 | 24 | 100% |
| 任务管理 | 9 | 27 | 100% |
| 统计分析 | 3 | 9 | 100% |
| **总计** | **20** | **60** | **100%** |

### 测试类型

- ✅ **功能测试**：验证所有接口功能正常
- ✅ **参数校验测试**：验证参数验证逻辑
- ✅ **权限测试**：验证权限控制
- ✅ **异常测试**：验证异常处理
- ✅ **业务逻辑测试**：验证状态流转、进度计算、得分计算

---

## 🔧 配置说明

### 测试环境配置

在运行测试前，请确保以下配置正确：

```yaml
# 服务配置
server:
  host: localhost
  port: 8084
  base-url: http://localhost:8084

# 认证配置（根据实际情况修改）
auth:
  enabled: true
  token: "your-access-token"
  
# 测试用户
test-user:
  username: "test"
  password: "test123"
  user-id: 1
```

---

## 📝 测试报告

测试完成后，查看测试报告：

- **Shell脚本测试**：查看终端输出
- **Postman测试**：导出测试报告
- **Swagger测试**：手动记录测试结果

---

## ⚠️ 注意事项

1. **测试数据隔离**：建议使用独立的测试数据库
2. **权限配置**：确保测试用户已分配所有权限
3. **数据清理**：测试完成后清理测试数据
4. **并发测试**：注意并发测试可能影响数据一致性

---

## 📞 问题反馈

如有问题，请联系开发团队或提交Issue。

---

**最后更新时间**：2026-01-11
**文档版本**：v1.0.0
