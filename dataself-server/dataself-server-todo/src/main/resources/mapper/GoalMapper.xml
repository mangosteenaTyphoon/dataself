<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shanzhu.dataself.server.todo.mapper.GoalMapper">

    <resultMap type="com.shanzhu.dataself.api.todo.domain.Goal" id="GoalResult">
        <id property="goalId" column="goal_id"/>
        <result property="userId" column="user_id"/>
        <result property="title" column="title"/>
        <result property="description" column="description"/>
        <result property="status" column="status"/>
        <result property="priority" column="priority"/>
        <result property="progress" column="progress"/>
        <result property="score" column="score"/>
        <result property="expectedStartTime" column="expected_start_time"/>
        <result property="expectedEndTime" column="expected_end_time"/>
        <result property="actualStartTime" column="actual_start_time"/>
        <result property="actualEndTime" column="actual_end_time"/>
        <result property="summary" column="summary"/>
        <result property="tags" column="tags"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectGoalVo">
        select goal_id, user_id, title, description, status, priority, progress, score,
               expected_start_time, expected_end_time, actual_start_time, actual_end_time,
               summary, tags, create_by, create_time, update_by, update_time, remark
        from todo_goal
    </sql>

    <select id="selectGoalList" parameterType="com.shanzhu.dataself.api.todo.domain.Goal" resultMap="GoalResult">
        <include refid="selectGoalVo"/>
        where del_flag = '0'
        <if test="userId != null">
            AND user_id = #{userId}
        </if>
        <if test="title != null and title != ''">
            AND title like concat('%', #{title}, '%')
        </if>
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        <if test="priority != null and priority != ''">
            AND priority = #{priority}
        </if>
        <if test="params.beginTime != null and params.beginTime != ''">
            AND date_format(create_time,'%y%m%d') &gt;= date_format(#{params.beginTime},'%y%m%d')
        </if>
        <if test="params.endTime != null and params.endTime != ''">
            AND date_format(create_time,'%y%m%d') &lt;= date_format(#{params.endTime},'%y%m%d')
        </if>
        <!-- 数据范围过滤 -->
        ${params.dataScope}
        order by create_time desc
    </select>

    <select id="selectGoalById" parameterType="Long" resultMap="GoalResult">
        <include refid="selectGoalVo"/>
        where goal_id = #{goalId}
    </select>

    <insert id="insertGoal" parameterType="com.shanzhu.dataself.api.todo.domain.Goal" useGeneratedKeys="true" keyProperty="goalId">
        insert into todo_goal(
        <if test="userId != null">user_id,</if>
        <if test="title != null and title != ''">title,</if>
        <if test="description != null and description != ''">description,</if>
        <if test="status != null and status != ''">status,</if>
        <if test="priority != null and priority != ''">priority,</if>
        <if test="progress != null">progress,</if>
        <if test="score != null">score,</if>
        <if test="expectedStartTime != null">expected_start_time,</if>
        <if test="expectedEndTime != null">expected_end_time,</if>
        <if test="actualStartTime != null">actual_start_time,</if>
        <if test="actualEndTime != null">actual_end_time,</if>
        <if test="summary != null and summary != ''">summary,</if>
        <if test="tags != null and tags != ''">tags,</if>
        <if test="createBy != null and createBy != ''">create_by,</if>
        <if test="remark != null and remark != ''">remark,</if>
        create_time
        )values(
        <if test="userId != null">#{userId},</if>
        <if test="title != null and title != ''">#{title},</if>
        <if test="description != null and description != ''">#{description},</if>
        <if test="status != null and status != ''">#{status},</if>
        <if test="priority != null and priority != ''">#{priority},</if>
        <if test="progress != null">#{progress},</if>
        <if test="score != null">#{score},</if>
        <if test="expectedStartTime != null">#{expectedStartTime},</if>
        <if test="expectedEndTime != null">#{expectedEndTime},</if>
        <if test="actualStartTime != null">#{actualStartTime},</if>
        <if test="actualEndTime != null">#{actualEndTime},</if>
        <if test="summary != null and summary != ''">#{summary},</if>
        <if test="tags != null and tags != ''">#{tags},</if>
        <if test="createBy != null and createBy != ''">#{createBy},</if>
        <if test="remark != null and remark != ''">#{remark},</if>
        sysdate()
        )
    </insert>

    <update id="updateGoal" parameterType="com.shanzhu.dataself.api.todo.domain.Goal">
        update todo_goal
        <set>
            <if test="title != null and title != ''">title = #{title},</if>
            <if test="description != null and description != ''">description = #{description},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="priority != null and priority != ''">priority = #{priority},</if>
            <if test="progress != null">progress = #{progress},</if>
            <if test="score != null">score = #{score},</if>
            <if test="expectedStartTime != null">expected_start_time = #{expectedStartTime},</if>
            <if test="expectedEndTime != null">expected_end_time = #{expectedEndTime},</if>
            <if test="actualStartTime != null">actual_start_time = #{actualStartTime},</if>
            <if test="actualEndTime != null">actual_end_time = #{actualEndTime},</if>
            <if test="summary != null">summary = #{summary},</if>
            <if test="tags != null">tags = #{tags},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            <if test="remark != null">remark = #{remark},</if>
            update_time = sysdate()
        </set>
        where goal_id = #{goalId}
    </update>

    <update id="updateGoalProgress">
        update todo_goal set progress = #{progress}, update_time = sysdate()
        where goal_id = #{goalId}
    </update>

    <update id="updateGoalScore">
        update todo_goal set score = #{score}, update_time = sysdate()
        where goal_id = #{goalId}
    </update>

    <update id="updateGoalStatus">
        update todo_goal set status = #{status}, update_time = sysdate()
        where goal_id = #{goalId}
    </update>

    <delete id="deleteGoalByIds" parameterType="Long">
        update todo_goal set del_flag = '2' where goal_id in
        <foreach collection="array" item="goalId" open="(" separator="," close=")">
            #{goalId}
        </foreach>
    </delete>

    <select id="countGoalsByUserId" parameterType="Long" resultType="int">
        select count(1) from todo_goal
        where user_id = #{userId} and del_flag = '0'
    </select>

    <select id="countGoalsByUserIdAndStatus" resultType="int">
        select count(1) from todo_goal
        where user_id = #{userId} and status = #{status} and del_flag = '0'
    </select>

    <select id="getAverageScoreByUserId" parameterType="Long" resultType="java.math.BigDecimal">
        select avg(score) from todo_goal
        where user_id = #{userId} and score is not null and del_flag = '0'
    </select>

</mapper>
